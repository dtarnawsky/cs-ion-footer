import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'cs-tabs-anim',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
